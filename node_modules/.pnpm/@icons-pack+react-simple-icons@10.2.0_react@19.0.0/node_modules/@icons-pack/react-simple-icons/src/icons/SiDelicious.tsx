
    import * as React from 'react';

    import { IconType } from '../types';

    type SiDeliciousProps = React.ComponentPropsWithoutRef<'svg'> & {
      /**
       * The title provides an accessible short text description to the SVG
       */
      title?: string;
      /**
       * Hex color or color name or "default" to use the default hex for each icon
       */
      color?: string;
      /**
       * The size of the Icon.
       */
      size?: string | number;
    }

    const defaultColor = '#0000FF';

    const SiDelicious: IconType = React.forwardRef<SVGSVGElement, SiDeliciousProps>(function SiDelicious({title = 'del.icio.us', color = 'currentColor', size = 24, ...others }, ref) {
      if (color === 'default') {
        color = defaultColor;
      }

      return (
        <svg
          xmlns='http://www.w3.org/2000/svg'
          width={size}
          height={size}
          fill={color}
          viewBox='0 0 24 24'
          ref={ref}
          {...others}
        >
          <title>{title}</title>
          <path d='M12 12H0v12h12V12zM24 0H12v12h12V0z' />
        </svg>
      );
    });

    export { SiDelicious as default, defaultColor };
  